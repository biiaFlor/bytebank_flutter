name: bytebank
description: Bytebank.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite:
  path:
  cupertino_icons: ^0.1.2
  http:
  http_interceptor:

dev_dependencies:
  flutter_test:
    sdk: flutter

module:
    androidX: true

flutter:
  uses-material-design: true

  assets:
    - images/bytebank_logo.png
